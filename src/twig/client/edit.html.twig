{% extends "common.html.twig" %}

{#====================================================================================================================#}
{# CLIENT - ADD                                                                                                       #}
{#                                                                                                                    #}
{# NOTE: Currently, no personalization is handled by the Client Template system.                                      #}
{#                                                                                                                    #}
{#====================================================================================================================#}

{% block body %}
    {# Email Subject #}
    {% if client.getIsLead %}
        {{ setSubject("Client Lead Edited"|translate) }}
    {% else %}
        {{ setSubject("Client Edited"|translate) }}
    {% endif %}

    <div>
        {# Salutation #}
        <p>
            {{ "Dear Sales Team,"|translate }}
        </p>

        {# Content #}
        <p>
            {% if client.getIsLead %}
                {{ "The following Client Lead has been edited in the UCRM."|translate }}
            {% else %}
                {{ "The following Client has been edited in the UCRM."|translate }}
            {% endif %}
        </p>

        {# Widget: Client #}
        {% include "_partials/client.html.twig" %}

        {# Widget: Map #}
        {% include "_partials/map.html.twig" %}
    </div>
{% endblock %}